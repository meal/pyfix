DBHOST = "localhost" 	   # host which to mysql should connect (not sure if CIDR works, so i'm using hostnames)
DBUSER = "postfix"	   # user as which we are connecting
DBPASS = "secret"	   # password for database
DBNAME = "postfix"   	   # name of the database
VERSION = "0.1"
